name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - dev

jobs:
  check:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Quality gate
        run: ./scripts/ci-check.sh
